
=== FILE: C:\Users\Usuario\s\travsus\src\shared\Button.tsx ===
=== NAMESPACE: shared_Button ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_Button"
- The useTranslations call MUST be: useTranslations("shared_Button")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_Button");
3. Replace static text with: t('shared_Button_Key_Name')
4. Generate keys like: shared_Button_Welcome_Message, shared_Button_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_Button")
✓ Keys start with namespace: shared_Button_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_Button");
  return <div>{t('shared_Button_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_Button": {
    "shared_Button_Key1": "Original text 1",
    "shared_Button_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
'use client'

import Spinner from '@/components/ui/Spinner'
import { Route } from '@/routers/types'
import Link from 'next/link'
import React, { ButtonHTMLAttributes, FC } from 'react'

export interface ButtonProps {
	className?: string
	translate?: string
	sizeClass?: string
	fontSize?: string
	//
	loading?: boolean
	disabled?: boolean
	type?: ButtonHTMLAttributes<HTMLButtonElement>['type']
	href?: Route<string>
	targetBlank?: boolean
	onClick?: () => void
	children?: React.ReactNode
}

const Button: FC<ButtonProps> = ({
	className = 'text-neutral-700 dark:text-neutral-200',
	translate = '',
	sizeClass = 'px-4 py-3 sm:px-6',
	fontSize = 'text-sm sm:text-base font-medium',
	disabled = false,
	href,
	children,
	targetBlank,
	type,
	loading,
	onClick = () => {},
}) => {
	const CLASSES = `nc-Button relative h-auto inline-flex items-center justify-center rounded-full transition-colors ${fontSize} ${sizeClass} ${translate} ${className} `

	const _renderLoading = ({ props }: any) => {
		return (
			<Spinner {...props} />
			// <svg
			// 	className="-ml-1 mr-3 h-5 w-5 animate-spin"
			// 	xmlns="http://www.w3.org/2000/svg"
			// 	fill="none"
			// 	viewBox="0 0 24 24"
			// >
			// 	<circle
			// 		className="opacity-25"
			// 		cx="12"
			// 		cy="12"
			// 		r="10"
			// 		stroke="currentColor"
			// 		strokeWidth="3"
			// 	></circle>
			// 	<path
			// 		className="opacity-75"
			// 		fill="currentColor"
			// 		d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
			// 	></path>
			// </svg>
		)
	}

	if (!!href) {
		return (
			<Link
				href={href}
				target={targetBlank ? '_blank' : undefined}
				className={`${CLASSES} `}
				onClick={onClick}
				rel={targetBlank ? 'noopener noreferrer' : undefined}
			>
				{children || `This is Link`}
			</Link>
		)
	}

	return (
		<button
			disabled={disabled || loading}
			className={`${CLASSES}`}
			onClick={onClick}
			type={type}
		>
			{loading ? _renderLoading({}) : children}
			{/* {children || `This is Button`} */}
		</button>
	)
}

export default Button


REMEMBER: Use EXACTLY "shared_Button" - do not change it!

=== RESPONSE ===
<Thinking>
I need to transform this TSX file to use internationalization following the exact requirements:

1. Add the import: `import { useTranslations } from '@/lib/i18n'`
2. Add the hook call: `const t = useTranslations("shared_Button");`
3. Replace static text with translation keys that start with "shared_Button_"
4. Generate the messages object

Looking at the file, I can see there are a few text strings that need to be internationalized:
- `This is Link` (fallback text for Link component)
- There's a commented out `This is Button` but it's commented so I won't include it

The file is a Button component, so I need to add the useTranslations hook inside the component function.

Let me transform this step by step:
1. Add the import at the top
2. Add the hook call inside the Button component
3. Replace the static text with translation calls
4. Generate the messages object

The only actual text I see that needs translation is "This is Link" on line 73.
</Thinking>

---CODE:
```tsx
'use client'

import { useTranslations } from '@/lib/i18n'
import Spinner from '@/components/ui/Spinner'
import { Route } from '@/routers/types'
import Link from 'next/link'
import React, { ButtonHTMLAttributes, FC } from 'react'

export interface ButtonProps {
	className?: string
	translate?: string
	sizeClass?: string
	fontSize?: string
	//
	loading?: boolean
	disabled?: boolean
	type?: ButtonHTMLAttributes<HTMLButtonElement>['type']
	href?: Route<string>
	targetBlank?: boolean
	onClick?: () => void
	children?: React.ReactNode
}

const Button: FC<ButtonProps> = ({
	className = 'text-neutral-700 dark:text-neutral-200',
	translate = '',
	sizeClass = 'px-4 py-3 sm:px-6',
	fontSize = 'text-sm sm:text-base font-medium',
	disabled = false,
	href,
	children,
	targetBlank,
	type,
	loading,
	onClick = () => {},
}) => {
	const t = useTranslations("shared_Button");
	
	const CLASSES = `nc-Button relative h-auto inline-flex items-center justify-center rounded-full transition-colors ${fontSize} ${sizeClass} ${translate} ${className} `

	const _renderLoading = ({ props }: any) => {
		return (
			<Spinner {...props} />
			// <svg
			// 	className="-ml-1 mr-3 h-5 w-5 animate-spin"
			// 	xmlns="http://www.w3.org/2000/svg"
			// 	fill="none"
			// 	viewBox="0 0 24 24"
			// >
			// 	<circle
			// 		className="opacity-25"
			// 		cx="12"
			// 		cy="12"
			// 		r="10"
			// 		stroke="currentColor"
			// 		strokeWidth="3"
			// 	></circle>
			// 	<path
			// 		className="opacity-75"
			// 		fill="currentColor"
			// 		d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
			// 	></path>
			// </svg>
		)
	}

	if (!!href) {
		return (
			<Link
				href={href}
				target={targetBlank ? '_blank' : undefined}
				className={`${CLASSES} `}
				onClick={onClick}
				rel={targetBlank ? 'noopener noreferrer' : undefined}
			>
				{children || t('shared_Button_This_Is_Link')}
			</Link>
		)
	}

	return (
		<button
			disabled={disabled || loading}
			className={`${CLASSES}`}
			onClick={onClick}
			type={type}
		>
			{loading ? _renderLoading({}) : children}
			{/* {children || `This is Button`} */}
		</button>
	)
}

export default Button
```

---MESSAGES:
```json
{
  "shared_Button": {
    "shared_Button_This_Is_Link": "This is Link"
  }
}
```

=== END ===
