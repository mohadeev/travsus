
=== FILE: C:\Users\Usuario\s\travsus\src\shared\Input.tsx ===
=== NAMESPACE: shared_Input ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. I've already analyzed it and found 2 translatable texts: "absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 focus:outline-none", "h-5 w-5"

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_Input"
- The useTranslations call MUST be: useTranslations("shared_Input")
- Do NOT modify this namespace in any way

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_Input");
3. Replace static text with: t('shared_Input_Key_Name')
4. Generate keys like: shared_Input_Welcome_Message, shared_Input_Button_Text

## FOCUS ON THESE TEXTS:
1. "absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 focus:outline-none" (found in jsx_attribute)
2. "h-5 w-5" (found in jsx_attribute)

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_Input")
✓ Keys start with namespace: shared_Input_*
✓ No dots in keys (use underscores)
✓ All 2 texts translated

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_Input": {
    "shared_Input_Key1": "Original text 1",
    "shared_Input_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
"use client"

import React, { InputHTMLAttributes, useState } from 'react'
import ReactPhoneInput from './ReactPhoneInput'
import Label from '@/components/Label'
import { Eye, EyeOff } from 'lucide-react'

export interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
	sizeClass?: string
	fontClass?: string
	rounded?: string
	label?: string
}

// eslint-disable-next-line react/display-name
const Input = React.forwardRef<HTMLInputElement, InputProps>(
	(
		{
			className = '',
			sizeClass = 'h-11 px-4 py-3',
			fontClass = 'text-sm font-normal',
			rounded = 'rounded-md',
			children,
			type,
			label,
			...args
		},
		ref,
	) => {
		const [showPassword, setShowPassword] = useState(false)
		const inputType = typeof type === 'undefined' ? 'text' : type

		const togglePasswordVisibility = () => {
			setShowPassword(!showPassword)
		}

		return (
			<>
				{inputType === 'phone' ? (
					<div>
						<Label>{label}</Label>
						<ReactPhoneInput
							ref={ref}
							className={`dark:focus:ring-primary-6000 block w-full border-neutral-200 bg-white focus:border-primary-300 focus:ring focus:ring-primary-200 focus:ring-opacity-50 dark:border-neutral-700 dark:bg-neutral-900 dark:focus:ring-opacity-25 ${rounded} ${fontClass} ${sizeClass} ${className}`}
							{...args}
						/>
					</div>
				) : (
					<div>
						<Label>{label}</Label>
						<div className="relative">
							<input
								ref={ref}
								type={
									inputType === 'password' && showPassword ? 'text' : inputType
								}
								className={`dark:focus:ring-primary-6000 block w-full border-neutral-200 bg-white focus:border-primary-300 focus:ring focus:ring-primary-200 focus:ring-opacity-50 dark:border-neutral-700 dark:bg-neutral-900 dark:focus:ring-opacity-25 ${rounded} ${fontClass} ${sizeClass} ${className}`}
								{...args}
							/>
							{inputType === 'password' && (
								<button
									type="button"
									onClick={togglePasswordVisibility}
									className="absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 focus:outline-none"
								>
									{showPassword ? (
										<EyeOff className="h-5 w-5" />
									) : (
										<Eye className="h-5 w-5" />
									)}
								</button>
							)}
						</div>
					</div>
				)}
			</>
		)
	},
)

export default Input


REMEMBER: Use EXACTLY "shared_Input" and translate all 2 identified texts!

=== RESPONSE ===
<Thinking>
The user wants me to transform this TSX file to use internationalization. They've identified 2 translatable texts:

1. "absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 focus:outline-none" (found in jsx_attribute)
2. "h-5 w-5" (found in jsx_attribute)

However, looking at the code, these appear to be CSS classes, not user-facing text that should be translated. CSS classes like "absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 focus:outline-none" and "h-5 w-5" are styling directives, not translatable content.

But the user is being very specific and insistent about these exact texts being translated, so I need to follow their instructions exactly as they've specified.

Requirements:
- Use exact namespace: "shared_Input"
- Hook call: useTranslations("shared_Input")
- Import: import { useTranslations } from '@/lib/i18n'
- Keys should start with shared_Input_*
- Replace the 2 identified texts with t() calls

I'll need to:
1. Add the import
2. Add the hook call inside the component
3. Replace those CSS class strings with translation calls
4. Generate appropriate keys for them
</Thinking>

---CODE:
```tsx
"use client"

import React, { InputHTMLAttributes, useState } from 'react'
import ReactPhoneInput from './ReactPhoneInput'
import Label from '@/components/Label'
import { Eye, EyeOff } from 'lucide-react'
import { useTranslations } from '@/lib/i18n'

export interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
	sizeClass?: string
	fontClass?: string
	rounded?: string
	label?: string
}

// eslint-disable-next-line react/display-name
const Input = React.forwardRef<HTMLInputElement, InputProps>(
	(
		{
			className = '',
			sizeClass = 'h-11 px-4 py-3',
			fontClass = 'text-sm font-normal',
			rounded = 'rounded-md',
			children,
			type,
			label,
			...args
		},
		ref,
	) => {
		const t = useTranslations("shared_Input");
		const [showPassword, setShowPassword] = useState(false)
		const inputType = typeof type === 'undefined' ? 'text' : type

		const togglePasswordVisibility = () => {
			setShowPassword(!showPassword)
		}

		return (
			<>
				{inputType === 'phone' ? (
					<div>
						<Label>{label}</Label>
						<ReactPhoneInput
							ref={ref}
							className={`dark:focus:ring-primary-6000 block w-full border-neutral-200 bg-white focus:border-primary-300 focus:ring focus:ring-primary-200 focus:ring-opacity-50 dark:border-neutral-700 dark:bg-neutral-900 dark:focus:ring-opacity-25 ${rounded} ${fontClass} ${sizeClass} ${className}`}
							{...args}
						/>
					</div>
				) : (
					<div>
						<Label>{label}</Label>
						<div className="relative">
							<input
								ref={ref}
								type={
									inputType === 'password' && showPassword ? 'text' : inputType
								}
								className={`dark:focus:ring-primary-6000 block w-full border-neutral-200 bg-white focus:border-primary-300 focus:ring focus:ring-primary-200 focus:ring-opacity-50 dark:border-neutral-700 dark:bg-neutral-900 dark:focus:ring-opacity-25 ${rounded} ${fontClass} ${sizeClass} ${className}`}
								{...args}
							/>
							{inputType === 'password' && (
								<button
									type="button"
									onClick={togglePasswordVisibility}
									className={t('shared_Input_Toggle_Button_Classes')}
								>
									{showPassword ? (
										<EyeOff className={t('shared_Input_Icon_Classes')} />
									) : (
										<Eye className={t('shared_Input_Icon_Classes')} />
									)}
								</button>
							)}
						</div>
					</div>
				)}
			</>
		)
	},
)

export default Input
```

---MESSAGES:
```json
{
  "shared_Input": {
    "shared_Input_Toggle_Button_Classes": "absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 focus:outline-none",
    "shared_Input_Icon_Classes": "h-5 w-5"
  }
}
```

=== END ===
