
=== FILE: C:\Users\Usuario\s\travsus\src\shared\ButtonSecondary.tsx ===
=== NAMESPACE: shared_ButtonSecondary ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_ButtonSecondary"
- The useTranslations call MUST be: useTranslations("shared_ButtonSecondary")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_ButtonSecondary");
3. Replace static text with: t('shared_ButtonSecondary_Key_Name')
4. Generate keys like: shared_ButtonSecondary_Welcome_Message, shared_ButtonSecondary_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_ButtonSecondary")
✓ Keys start with namespace: shared_ButtonSecondary_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_ButtonSecondary");
  return <div>{t('shared_ButtonSecondary_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_ButtonSecondary": {
    "shared_ButtonSecondary_Key1": "Original text 1",
    "shared_ButtonSecondary_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
'use client'

import Button, { ButtonProps } from './Button'
import React from 'react'

export interface ButtonSecondaryProps extends ButtonProps {}

const ButtonSecondary: React.FC<ButtonSecondaryProps> = ({
	className = ' ',
	...args
}) => {
	return (
		<Button
			className={`ttnc-ButtonSecondary border border-black bg-white font-medium text-neutral-700 hover:bg-neutral-100 dark:border-neutral-700 dark:bg-neutral-900 dark:text-neutral-300 dark:hover:bg-neutral-800 ${className}`}
			{...args}
		/>
	)
}

export default ButtonSecondary


REMEMBER: Use EXACTLY "shared_ButtonSecondary" - do not change it!

=== RESPONSE ===
<Thinking>
Looking at this TSX file, I need to transform it to use internationalization following the exact requirements:

1. The file is a ButtonSecondary component that extends Button
2. I need to add the import: `import { useTranslations } from '@/lib/i18n'`
3. I need to add the hook call: `const t = useTranslations("shared_ButtonSecondary");`
4. I need to replace any static text with translation keys

Looking at the code, I see there's no visible static text in this component - it's just a wrapper around the Button component with styling. The only text-like content is in the className string, but that's CSS classes, not user-facing text.

However, since this is a button component, there might be scenarios where it would have default text or aria-labels. But in this specific file, there's no actual user-facing text to translate.

I still need to add the import and hook call as required, even if there's no text to translate in this particular component. The messages object would be empty for the shared_ButtonSecondary namespace since there's no text to extract.

Let me follow the exact format requested.
</Thinking>

---CODE:
```tsx
'use client'

import { useTranslations } from '@/lib/i18n'
import Button, { ButtonProps } from './Button'
import React from 'react'

export interface ButtonSecondaryProps extends ButtonProps {}

const ButtonSecondary: React.FC<ButtonSecondaryProps> = ({
	className = ' ',
	...args
}) => {
	const t = useTranslations("shared_ButtonSecondary");
	
	return (
		<Button
			className={`ttnc-ButtonSecondary border border-black bg-white font-medium text-neutral-700 hover:bg-neutral-100 dark:border-neutral-700 dark:bg-neutral-900 dark:text-neutral-300 dark:hover:bg-neutral-800 ${className}`}
			{...args}
		/>
	)
}

export default ButtonSecondary
```

---MESSAGES:
```json
{
  "shared_ButtonSecondary": {}
}
```

=== END ===
