
=== FILE: C:\Users\Usuario\s\travsus\src\shared\Nav.tsx ===
=== NAMESPACE: shared_Nav ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_Nav"
- The useTranslations call MUST be: useTranslations("shared_Nav")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_Nav");
3. Replace static text with: t('shared_Nav_Key_Name')
4. Generate keys like: shared_Nav_Welcome_Message, shared_Nav_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_Nav")
✓ Keys start with namespace: shared_Nav_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_Nav");
  return <div>{t('shared_Nav_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_Nav": {
    "shared_Nav_Key1": "Original text 1",
    "shared_Nav_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import React, { FC } from "react";

export interface NavProps {
  containerClassName?: string;
  className?: string;
  children?: React.ReactNode;
}

const Nav: FC<NavProps> = ({
  containerClassName = "",
  className = "",
  children,
}) => {
  return (
    <nav className={`nc-Nav ${containerClassName}`} data-nc-id="Nav">
      <ul className={`flex  ${className}`}>{children}</ul>
    </nav>
  );
};

export default Nav;


REMEMBER: Use EXACTLY "shared_Nav" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform this TSX file to use internationalization with very specific requirements:

1. Must use the exact namespace "shared_Nav"
2. Must add the import: `import { useTranslations } from '@/lib/i18n'`
3. Must add the hook call: `const t = useTranslations("shared_Nav");`
4. Must replace static text with `t('shared_Nav_Key_Name')`
5. Keys should be like `shared_Nav_Welcome_Message`, `shared_Nav_Button_Text`

Looking at the provided TSX file, I see it's a Nav component that doesn't actually contain any visible text content that needs to be internationalized. The component only has:
- JSX structure with className attributes
- A data attribute `data-nc-id="Nav"`
- No user-facing text strings

Since there are no text strings to internationalize in this component, I still need to follow the instructions and add the required import and hook call, but there won't be any actual text replacements or translation keys needed.

However, the user is very strict about following the format exactly, so I should still provide the complete transformation even if there are no strings to translate.
</Thinking>

---CODE:
```tsx
import React, { FC } from "react";
import { useTranslations } from '@/lib/i18n'

export interface NavProps {
  containerClassName?: string;
  className?: string;
  children?: React.ReactNode;
}

const Nav: FC<NavProps> = ({
  containerClassName = "",
  className = "",
  children,
}) => {
  const t = useTranslations("shared_Nav");
  
  return (
    <nav className={`nc-Nav ${containerClassName}`} data-nc-id="Nav">
      <ul className={`flex  ${className}`}>{children}</ul>
    </nav>
  );
};

export default Nav;
```

---MESSAGES:
```json
{
  "shared_Nav": {}
}
```

=== END ===
