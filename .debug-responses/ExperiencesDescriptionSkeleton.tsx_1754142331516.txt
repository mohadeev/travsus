
=== FILE: C:\Users\Usuario\s\travsus\src\app\[locale]\[continent]\[country]\[region]\[city]\[category]\[name]\(service-detail)\[listing-experiences-detail]\ExperiencesDescriptionSkeleton.tsx ===
=== NAMESPACE: app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization.

## CRITICAL NAMESPACE AND KEY REQUIREMENTS:
- You MUST use the exact namespace: "app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton"
- The useTranslations call MUST be: useTranslations("app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton")
- Keys should be simple descriptive names WITHOUT the namespace prefix
- Example: t('Welcome_Message') NOT t('app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton_Welcome_Message')

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton");
3. Replace static text with: t('Simple_Key_Name')
4. Generate simple keys like: Welcome_Message, Button_Text, Error_Message

## KEY FORMAT RULES:
- Use descriptive names: Welcome_Message, Submit_Button, Error_Text
- NO namespace prefix in keys
- Use Title_Case_With_Underscores
- Keep keys under 30 characters

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton": {
	"Welcome_Message": "Welcome to our site",
	"Button_Text": "Click here",
	"Error_Message": "Something went wrong"
  }
}

## EXAMPLE TRANSFORMATION:

BEFORE:
<div>
  <h1>Welcome to our site</h1>
  <button>Click here</button>
  <p>Something went wrong</p>
</div>

AFTER:
import { useTranslations } from '@/lib/i18n';

export default function Component() {
  const t = useTranslations("app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton");
  
  return (
	<div>
	  <h1>{t('Welcome_Message')}</h1>
	  <button>{t('Button_Text')}</button>
	  <p>{t('Error_Message')}</p>
	</div>
  );
}

---MESSAGES:
{
  "app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton": {
	"Welcome_Message": "Welcome to our site",
	"Button_Text": "Click here", 
	"Error_Message": "Something went wrong"
  }
}

## FILE TO TRANSFORM:
import type React from 'react'

const ExperiencesDescriptionSkeleton: React.FC = (props: any) => {
	const { className } = props
	return (
		<div className={className}>
			<div className="space-y-8">
				<div className="h-8 w-64 animate-pulse rounded-md bg-gray-200" />
				<div className="space-y-4">
					{[...Array(4)].map((_, index) => (
						<div key={index} className="space-y-2">
							<div className="h-4 w-full animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-5/6 animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-4/6 animate-pulse rounded-md bg-gray-200" />
						</div>
					))}
				</div>
			</div>
		</div>
	)
}

export default ExperiencesDescriptionSkeleton


REMEMBER: 
- Use EXACTLY "app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton" in useTranslations()
- Keys should be simple names WITHOUT namespace prefix
- JSON structure: { "app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton": { "Simple_Key": "value" } }

=== RESPONSE ===
<Thinking>
Looking at this TSX file, I need to transform it to use internationalization. However, I notice that this is a skeleton component that only contains loading placeholders (animated divs with gray backgrounds) and no actual text content to translate.

The component is purely visual - it shows loading skeletons with:
- A title placeholder (h-8 w-64)
- Multiple paragraph placeholders in a loop

Since there's no static text to translate in this skeleton component, I don't need to add any translation keys. The component is just showing loading animations.

However, the user is asking me to follow the exact format requirements, so I should still add the import and useTranslations hook even though there's nothing to translate in this particular component.

Let me add the required imports and hook call, but since there's no text content, there won't be any translation keys to add.
</Thinking>

---CODE:
```tsx
import type React from 'react'
import { useTranslations } from '@/lib/i18n'

const ExperiencesDescriptionSkeleton: React.FC = (props: any) => {
	const { className } = props
	const t = useTranslations("app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton");
	
	return (
		<div className={className}>
			<div className="space-y-8">
				<div className="h-8 w-64 animate-pulse rounded-md bg-gray-200" />
				<div className="space-y-4">
					{[...Array(4)].map((_, index) => (
						<div key={index} className="space-y-2">
							<div className="h-4 w-full animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-5/6 animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-4/6 animate-pulse rounded-md bg-gray-200" />
						</div>
					))}
				</div>
			</div>
		</div>
	)
}

export default ExperiencesDescriptionSkeleton
```

---MESSAGES:
```json
{
  "app_locale_continent_country_region_city_category_name_servicedetail_listingexperiencesdetail_ExperiencesDescriptionSkeleton": {
  }
}
```

Note: This skeleton component contains no text content to translate - it only displays loading placeholders with animated gray backgrounds. The translation hook has been added as requested, but there are no translation keys needed since this is purely a visual loading component.

=== END ===
