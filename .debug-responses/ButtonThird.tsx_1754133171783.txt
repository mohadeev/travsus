
=== FILE: C:\Users\Usuario\s\travsus\src\shared\ButtonThird.tsx ===
=== NAMESPACE: shared_ButtonThird ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_ButtonThird"
- The useTranslations call MUST be: useTranslations("shared_ButtonThird")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_ButtonThird");
3. Replace static text with: t('shared_ButtonThird_Key_Name')
4. Generate keys like: shared_ButtonThird_Welcome_Message, shared_ButtonThird_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_ButtonThird")
✓ Keys start with namespace: shared_ButtonThird_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_ButtonThird");
  return <div>{t('shared_ButtonThird_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_ButtonThird": {
    "shared_ButtonThird_Key1": "Original text 1",
    "shared_ButtonThird_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import Button, { ButtonProps } from "./Button";
import React from "react";

export interface ButtonThirdProps extends ButtonProps {}

const ButtonThird: React.FC<ButtonThirdProps> = ({
  className = "text-neutral-700 border border-neutral-200 dark:text-neutral-200 dark:border-neutral-700",
  ...args
}) => {
  return <Button className={`ttnc-ButtonThird ${className}`} {...args} />;
};

export default ButtonThird;


REMEMBER: Use EXACTLY "shared_ButtonThird" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform a TSX file to use internationalization with very specific requirements. Let me analyze the file:

The file is a ButtonThird component that extends a Button component. Looking at the code, I need to:

1. Add the import: `import { useTranslations } from '@/lib/i18n'`
2. Add the hook call: `const t = useTranslations("shared_ButtonThird");`
3. Replace any static text with translation keys

However, looking at this specific file, I notice that there's actually no user-facing text content to translate. The component only:
- Imports Button and ButtonProps
- Defines an interface that extends ButtonProps
- Creates a component that renders a Button with specific className styling
- The only string is a CSS class name: `ttnc-ButtonThird ${className}`

CSS class names typically should NOT be internationalized as they are technical identifiers, not user-facing content.

But the user has given very strict instructions to follow the format exactly. Since there's no actual translatable text in this component, I should still add the required import and hook call to follow the instructions, even though there won't be any translation keys used in this particular case.

Wait, let me re-read the instructions. The user wants me to transform the file and provide the exact format with CODE and MESSAGES sections. Even if there's no translatable content, I should still follow the format.
</Thinking>

---CODE:
```tsx
import { useTranslations } from '@/lib/i18n'
import Button, { ButtonProps } from "./Button";
import React from "react";

export interface ButtonThirdProps extends ButtonProps {}

const ButtonThird: React.FC<ButtonThirdProps> = ({
  className = "text-neutral-700 border border-neutral-200 dark:text-neutral-200 dark:border-neutral-700",
  ...args
}) => {
  const t = useTranslations("shared_ButtonThird");
  return <Button className={`ttnc-ButtonThird ${className}`} {...args} />;
};

export default ButtonThird;
```

---MESSAGES:
```json
{
  "shared_ButtonThird": {}
}
```

=== END ===
