
=== FILE: C:\Users\Usuario\s\travsus\src\app\[locale]\test\page.tsx ===
=== NAMESPACE: app_locale_test_page ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "app_locale_test_page"
- The useTranslations call MUST be: useTranslations("app_locale_test_page")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("app_locale_test_page");
3. Replace static text with: t('app_locale_test_page_Key_Name')
4. Generate keys like: app_locale_test_page_Welcome_Message, app_locale_test_page_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("app_locale_test_page")
✓ Keys start with namespace: app_locale_test_page_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("app_locale_test_page");
  return <div>{t('app_locale_test_page_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "app_locale_test_page": {
    "app_locale_test_page_Key1": "Original text 1",
    "app_locale_test_page_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
'use client'

import type React from 'react'
import { useTranslations } from 'use-intl'

import { useState, useEffect } from 'react'
import { Plane, Calendar, RefreshCcw, ChevronDown } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { cn } from '@/lib/utils'

export default function HomeBanner() {
	const t = useTranslations('test_page')
	const [isVisible, setIsVisible] = useState(true)

	useEffect(() => {
		setIsVisible(true)
	}, [])

	const handlePageChange = () => {
		const experiencesContainer = document.getElementById(
			'experiences_container',
		)
		if (experiencesContainer) {
			experiencesContainer.scrollIntoView({
				behavior: 'smooth',
				block: 'start',
			})
		}
	}

	return (
		<div className="relative w-full py-12">
			<div className="relative z-20 flex min-h-[auto] flex-col items-center justify-center px-4 text-center sm:px-6 lg:px-8">
				<div
					className={cn(
						'max-w-5xl transition-all duration-1000',
						isVisible
							? 'translate-y-0 opacity-100'
							: 'translate-y-10 opacity-0',
					)}
				>
					<h1 className="font-serif mb-6 text-7xl font-extrabold tracking-tight text-black sm:text-5xl md:text-6xl lg:text-[100px] lg:font-extrabold">
						{t('test_page_Travel_With_Ultimate_Flexibility')}
					</h1>
					<p className="mx-auto mb-10 max-w-2xl text-lg font-medium text-black sm:text-xl md:text-2xl">
						{t('test_page_Book_Cancel_Refund_Anytime_Anywhere')}
					</p>
				</div>

				<div
					className={cn(
						'transition-all delay-500 duration-1000',
						isVisible
							? 'translate-y-0 opacity-100'
							: 'translate-y-10 opacity-0',
					)}
				>
					<Button
						size="lg"
						className="rounded-full bg-primary text-lg font-medium shadow-lg transition-all hover:bg-primary/90 hover:shadow-xl"
						onClick={handlePageChange}
					>
						{t('test_page_Plan_Your_Flexible_Trip')}
					</Button>
				</div>
			</div>
		</div>
	)
}

function FeatureItem({
	icon,
	title,
	description,
	onClick,
}: {
	icon: React.ReactNode
	title: string
	description: string
	onClick: () => void
}) {
	const t = useTranslations('test_page')

	return (
		<div
			className="group flex cursor-pointer flex-col items-center gap-3 rounded-xl bg-gray-50 p-6 transition-all hover:bg-gray-100 hover:shadow-lg"
			onClick={onClick}
		>
			<div className="rounded-full bg-primary/20 p-3 text-primary transition-all group-hover:bg-primary/30">
				{icon}
			</div>
			<div className="flex flex-col items-center">
				<span className="text-xl font-semibold text-black">{t(title)}</span>
				<span className="text-sm text-gray-600">{t(description)}</span>
			</div>
		</div>
	)
}


REMEMBER: Use EXACTLY "app_locale_test_page" - do not change it!

=== RESPONSE ===
<Thinking>
I need to transform this TSX file to use internationalization with the exact namespace "app_locale_test_page". Let me analyze the current file:

1. It's already using internationalization with `useTranslations('test_page')`
2. I need to change the import from 'use-intl' to '@/lib/i18n'
3. I need to change the namespace from 'test_page' to 'app_locale_test_page'
4. I need to update all the translation keys to start with 'app_locale_test_page_'
5. I need to identify all the translatable text and create appropriate keys

Current translatable content:
- `t('test_page_Travel_With_Ultimate_Flexibility')`
- `t('test_page_Book_Cancel_Refund_Anytime_Anywhere')`
- `t('test_page_Plan_Your_Flexible_Trip')`
- In FeatureItem component: `t(title)` and `t(description)` - these are passed as props

Let me transform this step by step:

1. Change import to: `import { useTranslations } from '@/lib/i18n'`
2. Change useTranslations calls to: `useTranslations("app_locale_test_page")`
3. Update all translation keys to start with `app_locale_test_page_`

For the keys, I'll convert:
- 'test_page_Travel_With_Ultimate_Flexibility' → 'app_locale_test_page_Travel_With_Ultimate_Flexibility'
- 'test_page_Book_Cancel_Refund_Anytime_Anywhere' → 'app_locale_test_page_Book_Cancel_Refund_Anytime_Anywhere'
- 'test_page_Plan_Your_Flexible_Trip' → 'app_locale_test_page_Plan_Your_Flexible_Trip'

For the FeatureItem component, the title and description are passed as props, so they would need to be translation keys as well.
</Thinking>

---CODE:
```tsx
'use client'

import type React from 'react'
import { useTranslations } from '@/lib/i18n'

import { useState, useEffect } from 'react'
import { Plane, Calendar, RefreshCcw, ChevronDown } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { cn } from '@/lib/utils'

export default function HomeBanner() {
	const t = useTranslations("app_locale_test_page")
	const [isVisible, setIsVisible] = useState(true)

	useEffect(() => {
		setIsVisible(true)
	}, [])

	const handlePageChange = () => {
		const experiencesContainer = document.getElementById(
			'experiences_container',
		)
		if (experiencesContainer) {
			experiencesContainer.scrollIntoView({
				behavior: 'smooth',
				block: 'start',
			})
		}
	}

	return (
		<div className="relative w-full py-12">
			<div className="relative z-20 flex min-h-[auto] flex-col items-center justify-center px-4 text-center sm:px-6 lg:px-8">
				<div
					className={cn(
						'max-w-5xl transition-all duration-1000',
						isVisible
							? 'translate-y-0 opacity-100'
							: 'translate-y-10 opacity-0',
					)}
				>
					<h1 className="font-serif mb-6 text-7xl font-extrabold tracking-tight text-black sm:text-5xl md:text-6xl lg:text-[100px] lg:font-extrabold">
						{t('app_locale_test_page_Travel_With_Ultimate_Flexibility')}
					</h1>
					<p className="mx-auto mb-10 max-w-2xl text-lg font-medium text-black sm:text-xl md:text-2xl">
						{t('app_locale_test_page_Book_Cancel_Refund_Anytime_Anywhere')}
					</p>
				</div>

				<div
					className={cn(
						'transition-all delay-500 duration-1000',
						isVisible
							? 'translate-y-0 opacity-100'
							: 'translate-y-10 opacity-0',
					)}
				>
					<Button
						size="lg"
						className="rounded-full bg-primary text-lg font-medium shadow-lg transition-all hover:bg-primary/90 hover:shadow-xl"
						onClick={handlePageChange}
					>
						{t('app_locale_test_page_Plan_Your_Flexible_Trip')}
					</Button>
				</div>
			</div>
		</div>
	)
}

function FeatureItem({
	icon,
	title,
	description,
	onClick,
}: {
	icon: React.ReactNode
	title: string
	description: string
	onClick: () => void
}) {
	const t = useTranslations("app_locale_test_page")

	return (
		<div
			className="group flex cursor-pointer flex-col items-center gap-3 rounded-xl bg-gray-50 p-6 transition-all hover:bg-gray-100 hover:shadow-lg"
			onClick={onClick}
		>
			<div className="rounded-full bg-primary/20 p-3 text-primary transition-all group-hover:bg-primary/30">
				{icon}
			</div>
			<div className="flex flex-col items-center">
				<span className="text-xl font-semibold text-black">{t(title)}</span>
				<span className="text-sm text-gray-600">{t(description)}</span>
			</div>
		</div>
	)
}
```

---MESSAGES:
```json
{
  "app_locale_test_page": {
    "app_locale_test_page_Travel_With_Ultimate_Flexibility": "Travel With Ultimate Flexibility",
    "app_locale_test_page_Book_Cancel_Refund_Anytime_Anywhere": "Book, Cancel, Refund Anytime, Anywhere",
    "app_locale_test_page_Plan_Your_Flexible_Trip": "Plan Your Flexible Trip"
  }
}
```

=== END ===
