
=== FILE: C:\Users\Usuario\s\travsus\src\components\StartRating.tsx ===
=== NAMESPACE: components_StartRating ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "components_StartRating"
- The useTranslations call MUST be: useTranslations("components_StartRating")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("components_StartRating");
3. Replace static text with: t('components_StartRating_Key_Name')
4. Generate keys like: components_StartRating_Welcome_Message, components_StartRating_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("components_StartRating")
✓ Keys start with namespace: components_StartRating_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("components_StartRating");
  return <div>{t('components_StartRating_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "components_StartRating": {
    "components_StartRating_Key1": "Original text 1",
    "components_StartRating_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import { StarIcon } from "@heroicons/react/24/solid";
import React, { FC } from "react";

export interface StartRatingProps {
  className?: string;
  point?: number;
  reviewCount?: number;
}

const StartRating: FC<StartRatingProps> = ({
  className = "",
  point = 5.0,
  reviewCount = 0,
}) => {
  return (
    <div
      className={`nc-StartRating flex items-center space-x-1 text-sm  ${className}`}
      data-nc-id="StartRating"
    >
      <div className="pb-[2px]">
        <StarIcon className="w-[18px] h-[18px] text-orange-500" />
      </div>
      <span className="font-medium ">{point}</span>
      <span className="text-neutral-500 dark:text-neutral-400">
        ({reviewCount})
      </span>
    </div>
  );
};

export default StartRating;


REMEMBER: Use EXACTLY "components_StartRating" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform a TSX file to use internationalization with very specific requirements:

1. Must use exact namespace: "components_StartRating"
2. Must add import: `import { useTranslations } from '@/lib/i18n'`
3. Must add hook call: `const t = useTranslations("components_StartRating");`
4. Must replace static text with t('components_StartRating_Key_Name')
5. Keys should follow the pattern: components_StartRating_*

Looking at the file, I need to identify what text needs to be internationalized. In this case, I see:
- The component displays a star rating with a point value and review count
- The text that's displayed is: `{point}` and `({reviewCount})`
- These are dynamic values, not static text that needs translation

Actually, looking more carefully, the only text content in this component is:
- `{point}` - this is a dynamic number
- `({reviewCount})` - this is also a dynamic number in parentheses

There's no actual static text to translate in this component. The component just displays dynamic values (point and reviewCount). However, the user is asking me to transform it for internationalization, so I should follow their instructions exactly.

Since there's no static text to translate, I'll still add the required import and hook call as instructed, but there won't be any actual translation keys needed since all the text is dynamic.

Wait, let me re-read the requirements. The user says "Replace static text with: t('components_StartRating_Key_Name')" but there is no static text in this component. All the text is dynamic (point and reviewCount variables).

I should still follow the format they requested and add the import and hook call, but explain that there's no static text to translate in this particular component.
</Thinking>

---CODE:
```tsx
import { StarIcon } from "@heroicons/react/24/solid";
import React, { FC } from "react";
import { useTranslations } from '@/lib/i18n';

export interface StartRatingProps {
  className?: string;
  point?: number;
  reviewCount?: number;
}

const StartRating: FC<StartRatingProps> = ({
  className = "",
  point = 5.0,
  reviewCount = 0,
}) => {
  const t = useTranslations("components_StartRating");
  
  return (
    <div
      className={`nc-StartRating flex items-center space-x-1 text-sm  ${className}`}
      data-nc-id="StartRating"
    >
      <div className="pb-[2px]">
        <StarIcon className="w-[18px] h-[18px] text-orange-500" />
      </div>
      <span className="font-medium ">{point}</span>
      <span className="text-neutral-500 dark:text-neutral-400">
        ({reviewCount})
      </span>
    </div>
  );
};

export default StartRating;
```

---MESSAGES:
```json
{
  "components_StartRating": {}
}
```

Note: This component only displays dynamic values (point and reviewCount) and contains no static text that requires translation. The internationalization setup has been added as requested, but no translation keys are needed for this particular component.

=== END ===
