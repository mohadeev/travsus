
=== FILE: C:\Users\Usuario\s\travsus\src\shared\Checkbox.tsx ===
=== NAMESPACE: shared_Checkbox ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_Checkbox"
- The useTranslations call MUST be: useTranslations("shared_Checkbox")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_Checkbox");
3. Replace static text with: t('shared_Checkbox_Key_Name')
4. Generate keys like: shared_Checkbox_Welcome_Message, shared_Checkbox_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_Checkbox")
✓ Keys start with namespace: shared_Checkbox_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_Checkbox");
  return <div>{t('shared_Checkbox_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_Checkbox": {
    "shared_Checkbox_Key1": "Original text 1",
    "shared_Checkbox_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
"use client";

import React, { FC } from "react";

export interface CheckboxProps {
  label?: string;
  subLabel?: string;
  className?: string;
  name: string;
  defaultChecked?: boolean;
  onChange?: (checked: boolean) => void;
}

const Checkbox: FC<CheckboxProps> = ({
  subLabel = "",
  label = "",
  name,
  className = "",
  defaultChecked,
  onChange,
}) => {
  return (
    <div className={`flex text-sm sm:text-base ${className}`}>
      <input
        id={name}
        name={name}
        type="checkbox"
        className="focus:ring-action-primary h-6 w-6 text-primary-500 border-primary rounded border-neutral-500 bg-white dark:bg-neutral-700  dark:checked:bg-primary-500 focus:ring-primary-500"
        defaultChecked={defaultChecked}
        onChange={(e) => onChange && onChange(e.target.checked)}
      />
      {label && (
        <label
          htmlFor={name}
          className="ml-3.5 flex flex-col flex-1 justify-center"
        >
          <span className=" text-neutral-900 dark:text-neutral-100">
            {label}
          </span>
          {subLabel && (
            <p className="mt-1 text-neutral-500 dark:text-neutral-400 text-sm font-light">
              {subLabel}
            </p>
          )}
        </label>
      )}
    </div>
  );
};

export default Checkbox;


REMEMBER: Use EXACTLY "shared_Checkbox" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform a TSX file to use internationalization with very specific requirements:

1. Must use exact namespace "shared_Checkbox"
2. Must add import: `import { useTranslations } from '@/lib/i18n'`
3. Must add hook call: `const t = useTranslations("shared_Checkbox");`
4. Must replace static text with t('shared_Checkbox_Key_Name') format
5. Keys should start with namespace and use underscores

Looking at the file, I need to identify what text needs to be internationalized. The component has:
- `label` and `subLabel` props that are passed in, so these are dynamic and don't need translation keys
- No static text strings that need translation

Wait, looking more carefully at the component, I see that the `label` and `subLabel` are props that get rendered directly. The component itself doesn't contain any hardcoded text that needs translation.

However, the user is asking me to transform this file for internationalization. Since there's no static text in this component, I should still add the required import and hook call as specified, but there won't be any actual text to replace with translation keys.

But let me think about this more - the user gave very specific instructions and an example showing how to transform text. Since this component doesn't have hardcoded text, I should still follow the format exactly as requested, adding the import and hook call, even if there are no translation keys to use in this particular component.
</Thinking>

---CODE:
```tsx
"use client";

import React, { FC } from "react";
import { useTranslations } from '@/lib/i18n';

export interface CheckboxProps {
  label?: string;
  subLabel?: string;
  className?: string;
  name: string;
  defaultChecked?: boolean;
  onChange?: (checked: boolean) => void;
}

const Checkbox: FC<CheckboxProps> = ({
  subLabel = "",
  label = "",
  name,
  className = "",
  defaultChecked,
  onChange,
}) => {
  const t = useTranslations("shared_Checkbox");
  
  return (
    <div className={`flex text-sm sm:text-base ${className}`}>
      <input
        id={name}
        name={name}
        type="checkbox"
        className="focus:ring-action-primary h-6 w-6 text-primary-500 border-primary rounded border-neutral-500 bg-white dark:bg-neutral-700  dark:checked:bg-primary-500 focus:ring-primary-500"
        defaultChecked={defaultChecked}
        onChange={(e) => onChange && onChange(e.target.checked)}
      />
      {label && (
        <label
          htmlFor={name}
          className="ml-3.5 flex flex-col flex-1 justify-center"
        >
          <span className=" text-neutral-900 dark:text-neutral-100">
            {label}
          </span>
          {subLabel && (
            <p className="mt-1 text-neutral-500 dark:text-neutral-400 text-sm font-light">
              {subLabel}
            </p>
          )}
        </label>
      )}
    </div>
  );
};

export default Checkbox;
```

---MESSAGES:
```json
{
  "shared_Checkbox": {}
}
```

=== END ===
