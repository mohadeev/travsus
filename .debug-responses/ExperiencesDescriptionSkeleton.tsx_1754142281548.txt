
=== FILE: C:\Users\Usuario\s\travsus\src\app\[locale]\[continent]\[country]\[region]\[city]\[category]\[name]\(service-detail)\[listing-experiences-detail]\ExperiencesDescriptionSkeleton.tsx ===
=== NAMESPACE: ExperiencesDescriptionSkeleton ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. I've provided the component code below.

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "ExperiencesDescriptionSkeleton"
- The useTranslations call MUST be: useTranslations("ExperiencesDescriptionSkeleton")
- Do NOT modify this namespace in any way

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("ExperiencesDescriptionSkeleton");
3. Replace static text with: t('ExperiencesDescriptionSkeleton_Key_Name')
4. Generate keys like: ExperiencesDescriptionSkeleton_Welcome_Message, ExperiencesDescriptionSkeleton_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("ExperiencesDescriptionSkeleton")
✓ Keys start with namespace: ExperiencesDescriptionSkeleton_*
✓ No dots in keys (use underscores)

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "ExperiencesDescriptionSkeleton": {
    "ExperiencesDescriptionSkeleton_Key1": "Original text 1",
    "ExperiencesDescriptionSkeleton_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import type React from 'react'

const ExperiencesDescriptionSkeleton: React.FC = (props: any) => {
	const { className } = props
	return (
		<div className={className}>
			<div className="space-y-8">
				<div className="h-8 w-64 animate-pulse rounded-md bg-gray-200" />
				<div className="space-y-4">
					{[...Array(4)].map((_, index) => (
						<div key={index} className="space-y-2">
							<div className="h-4 w-full animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-5/6 animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-4/6 animate-pulse rounded-md bg-gray-200" />
						</div>
					))}
				</div>
			</div>
		</div>
	)
}

export default ExperiencesDescriptionSkeleton


REMEMBER: Use EXACTLY "ExperiencesDescriptionSkeleton" and replace all user-facing text!

=== RESPONSE ===
<Thinking>
Looking at this TSX file, I need to analyze what text content needs to be internationalized. However, I notice that this is a skeleton loading component that only contains animated placeholder divs with no actual text content to translate.

The component consists of:
- Animated gray bars/rectangles as loading placeholders
- No user-facing text strings
- No labels, titles, or any textual content

Since this is purely a visual skeleton loader with no text content, there's actually nothing to internationalize. However, the user is asking me to follow their instructions exactly, so I need to:

1. Add the import: `import { useTranslations } from '@/lib/i18n'`
2. Add the hook call: `const t = useTranslations("ExperiencesDescriptionSkeleton");`
3. But there are no text strings to replace with t() calls

I should add the required imports and hook call as requested, but note that there's no actual text content to internationalize in this skeleton component.
</Thinking>

---CODE:
```tsx
import type React from 'react'
import { useTranslations } from '@/lib/i18n'

const ExperiencesDescriptionSkeleton: React.FC = (props: any) => {
	const { className } = props
	const t = useTranslations("ExperiencesDescriptionSkeleton");
	
	return (
		<div className={className}>
			<div className="space-y-8">
				<div className="h-8 w-64 animate-pulse rounded-md bg-gray-200" />
				<div className="space-y-4">
					{[...Array(4)].map((_, index) => (
						<div key={index} className="space-y-2">
							<div className="h-4 w-full animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-5/6 animate-pulse rounded-md bg-gray-200" />
							<div className="h-4 w-4/6 animate-pulse rounded-md bg-gray-200" />
						</div>
					))}
				</div>
			</div>
		</div>
	)
}

export default ExperiencesDescriptionSkeleton
```

---MESSAGES:
```json
{
  "ExperiencesDescriptionSkeleton": {
  }
}
```

Note: This skeleton component contains no user-facing text content to internationalize. It only renders animated placeholder elements for loading states. The required import and hook have been added as requested, but there are no text strings to replace with translation keys.

=== END ===
