
=== FILE: C:\Users\Usuario\s\travsus\src\shared\Heading.tsx ===
=== NAMESPACE: shared_Heading ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_Heading"
- The useTranslations call MUST be: useTranslations("shared_Heading")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_Heading");
3. Replace static text with: t('shared_Heading_Key_Name')
4. Generate keys like: shared_Heading_Welcome_Message, shared_Heading_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_Heading")
✓ Keys start with namespace: shared_Heading_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_Heading");
  return <div>{t('shared_Heading_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_Heading": {
    "shared_Heading_Key1": "Original text 1",
    "shared_Heading_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import React, { HTMLAttributes, ReactNode } from "react";
import  { FC } from 'react'

export interface HeadingProps extends HTMLAttributes<HTMLHeadingElement> {
  fontClass?: string;
  desc?: ReactNode;
  isCenter?: boolean;
}

const Heading: React.FC<HeadingProps> = ({
  children,
  desc = "Discover the most outstanding articles in all topics of life. ",
  className = "mb-10 text-neutral-900 dark:text-neutral-50",
  isCenter = false,
  ...args
}) => {
  return (
    <div className={`nc-Section-Heading relative ${className}`}>
      <div
        className={
          isCenter ? "text-center w-full max-w-2xl mx-auto mb-4" : "max-w-2xl"
        }
      >
        <h2 className={`text-3xl md:text-4xl font-semibold`} {...args}>
          {children || `Section Heading`}
        </h2>
        {desc && (
          <span className="block mt-2 md:mt-3 font-normal text-base sm:text-lg text-neutral-500 dark:text-neutral-400">
            {desc}
          </span>
        )}
      </div>
    </div>
  );
};

export default Heading;

export interface HeadingSkeletonProps {
  className?: string
  isCenter?: boolean
}
export const HeadingSkeleton : FC<HeadingSkeletonProps> = ({
  className = 'mb-10',
  isCenter = false,
}) => {
  return (
    <div className={`nc-Section-Heading relative ${className} animate-pulse`}>
      <div className={isCenter ? 'text-center w-full max-w-2xl mx-auto mb-4' : 'max-w-2xl'}>
        <div className={`h-8 bg-gray-300 rounded w-3/4 ${isCenter ? 'mx-auto' : ''}`}></div>
        <div className={`h-5 bg-gray-300 rounded w-1/2 mt-3 ${isCenter ? 'mx-auto' : ''}`}></div>
      </div>
    </div>
  )
}



REMEMBER: Use EXACTLY "shared_Heading" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform this TSX file to use internationalization with very specific requirements:

1. Must use exact namespace "shared_Heading"
2. Must use useTranslations("shared_Heading")
3. Must replace static text with t('shared_Heading_Key_Name') format
4. Must add the specific import
5. Must generate keys that start with shared_Heading_

Looking at the file, I can see the static text that needs to be internationalized:
1. The default desc prop: "Discover the most outstanding articles in all topics of life. "
2. The fallback children text: "Section Heading"

I need to:
1. Add the import: import { useTranslations } from '@/lib/i18n'
2. Add the hook call: const t = useTranslations("shared_Heading");
3. Replace the static strings with translation keys
4. Generate the messages object

The keys should be:
- shared_Heading_Default_Description for "Discover the most outstanding articles in all topics of life. "
- shared_Heading_Section_Heading for "Section Heading"

I need to make sure I follow the exact format specified.
</Thinking>

---CODE:
```tsx
import React, { HTMLAttributes, ReactNode } from "react";
import  { FC } from 'react'
import { useTranslations } from '@/lib/i18n'

export interface HeadingProps extends HTMLAttributes<HTMLHeadingElement> {
  fontClass?: string;
  desc?: ReactNode;
  isCenter?: boolean;
}

const Heading: React.FC<HeadingProps> = ({
  children,
  desc,
  className = "mb-10 text-neutral-900 dark:text-neutral-50",
  isCenter = false,
  ...args
}) => {
  const t = useTranslations("shared_Heading");
  
  return (
    <div className={`nc-Section-Heading relative ${className}`}>
      <div
        className={
          isCenter ? "text-center w-full max-w-2xl mx-auto mb-4" : "max-w-2xl"
        }
      >
        <h2 className={`text-3xl md:text-4xl font-semibold`} {...args}>
          {children || t('shared_Heading_Section_Heading')}
        </h2>
        {(desc !== undefined ? desc : t('shared_Heading_Default_Description')) && (
          <span className="block mt-2 md:mt-3 font-normal text-base sm:text-lg text-neutral-500 dark:text-neutral-400">
            {desc !== undefined ? desc : t('shared_Heading_Default_Description')}
          </span>
        )}
      </div>
    </div>
  );
};

export default Heading;

export interface HeadingSkeletonProps {
  className?: string
  isCenter?: boolean
}
export const HeadingSkeleton : FC<HeadingSkeletonProps> = ({
  className = 'mb-10',
  isCenter = false,
}) => {
  return (
    <div className={`nc-Section-Heading relative ${className} animate-pulse`}>
      <div className={isCenter ? 'text-center w-full max-w-2xl mx-auto mb-4' : 'max-w-2xl'}>
        <div className={`h-8 bg-gray-300 rounded w-3/4 ${isCenter ? 'mx-auto' : ''}`}></div>
        <div className={`h-5 bg-gray-300 rounded w-1/2 mt-3 ${isCenter ? 'mx-auto' : ''}`}></div>
      </div>
    </div>
  )
}
```

---MESSAGES:
```json
{
  "shared_Heading": {
    "shared_Heading_Default_Description": "Discover the most outstanding articles in all topics of life. ",
    "shared_Heading_Section_Heading": "Section Heading"
  }
}
```

=== END ===
