
=== FILE: C:\Users\Usuario\s\travsus\src\shared\ButtonClose.tsx ===
=== NAMESPACE: shared_ButtonClose ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_ButtonClose"
- The useTranslations call MUST be: useTranslations("shared_ButtonClose")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_ButtonClose");
3. Replace static text with: t('shared_ButtonClose_Key_Name')
4. Generate keys like: shared_ButtonClose_Welcome_Message, shared_ButtonClose_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_ButtonClose")
✓ Keys start with namespace: shared_ButtonClose_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_ButtonClose");
  return <div>{t('shared_ButtonClose_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_ButtonClose": {
    "shared_ButtonClose_Key1": "Original text 1",
    "shared_ButtonClose_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import React from "react";
import { XMarkIcon } from "@heroicons/react/24/solid";
import twFocusClass from "@/utils/twFocusClass";

export interface ButtonCloseProps {
  className?: string;
  onClick?: () => void;
}

const ButtonClose: React.FC<ButtonCloseProps> = ({
  className = "",
  onClick = () => {},
}) => {
  return (
    <button
      className={
        `w-8 h-8 flex items-center justify-center rounded-full text-neutral-700 dark:text-neutral-300 hover:bg-neutral-100 dark:hover:bg-neutral-700 ${className} ` +
        twFocusClass()
      }
      onClick={onClick}
    >
      <span className="sr-only">Close</span>
      <XMarkIcon className="w-5 h-5" />
    </button>
  );
};

export default ButtonClose;


REMEMBER: Use EXACTLY "shared_ButtonClose" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform a TSX file to use internationalization with very specific requirements:

1. Must use the exact namespace "shared_ButtonClose"
2. Must import from '@/lib/i18n'
3. Must use useTranslations("shared_ButtonClose")
4. Must replace static text with t() calls using keys that start with "shared_ButtonClose_"
5. Must use underscores, not dots in keys

Looking at the file, I can see there's only one piece of text that needs to be internationalized:
- "Close" in the sr-only span

I need to:
1. Add the import
2. Add the hook call inside the component
3. Replace "Close" with a translation key
4. Generate the messages object

The key should be something like "shared_ButtonClose_Close" for the "Close" text.

Since this is a functional component, I need to add the hook call inside the component function.
</Thinking>

---CODE:
```tsx
import React from "react";
import { XMarkIcon } from "@heroicons/react/24/solid";
import twFocusClass from "@/utils/twFocusClass";
import { useTranslations } from '@/lib/i18n';

export interface ButtonCloseProps {
  className?: string;
  onClick?: () => void;
}

const ButtonClose: React.FC<ButtonCloseProps> = ({
  className = "",
  onClick = () => {},
}) => {
  const t = useTranslations("shared_ButtonClose");
  
  return (
    <button
      className={
        `w-8 h-8 flex items-center justify-center rounded-full text-neutral-700 dark:text-neutral-300 hover:bg-neutral-100 dark:hover:bg-neutral-700 ${className} ` +
        twFocusClass()
      }
      onClick={onClick}
    >
      <span className="sr-only">{t('shared_ButtonClose_Close')}</span>
      <XMarkIcon className="w-5 h-5" />
    </button>
  );
};

export default ButtonClose;
```

---MESSAGES:
```json
{
  "shared_ButtonClose": {
    "shared_ButtonClose_Close": "Close"
  }
}
```

=== END ===
