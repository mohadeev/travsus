
=== FILE: C:\Users\Usuario\s\travsus\src\shared\SocialsShare.tsx ===
=== NAMESPACE: shared_SocialsShare ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. I've already analyzed it and found 2 translatable texts: "grid gap-[6px]", "nc-SocialsShare ${className}"

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_SocialsShare"
- The useTranslations call MUST be: useTranslations("shared_SocialsShare")
- Do NOT modify this namespace in any way

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_SocialsShare");
3. Replace static text with: t('shared_SocialsShare_Key_Name')
4. Generate keys like: shared_SocialsShare_Welcome_Message, shared_SocialsShare_Button_Text

## FOCUS ON THESE TEXTS:
1. "grid gap-[6px]" (found in jsx_attribute)
2. "nc-SocialsShare ${className}" (found in return_string)

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_SocialsShare")
✓ Keys start with namespace: shared_SocialsShare_*
✓ No dots in keys (use underscores)
✓ All 2 texts translated

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_SocialsShare": {
    "shared_SocialsShare_Key1": "Original text 1",
    "shared_SocialsShare_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import React, { FC } from 'react'

export interface SocialsShareProps {
	className?: string
	itemClass?: string
}

export interface SocialType {
	name: string
	icon: string
	href: string
}

const socials: SocialType[] = [
	// { name: "Facebook", icon: "lab la-facebook-f", href: "#" },
	// { name: "Twitter", icon: "lab la-twitter", href: "#" },
	// { name: "Linkedin", icon: "lab la-linkedin-in", href: "#" },
	{
		name: 'Instagram',
		icon: 'lab la-instagram',
		href: 'https://www.instagram.com/travsusofficial/',
	},
]

const SocialsShare: FC<SocialsShareProps> = ({
	className = 'grid gap-[6px]',
	itemClass = 'w-7 h-7 text-base hover:bg-neutral-100',
}) => {
	const renderItem = (item: SocialType, index: number) => {
		return (
			<a
				key={index}
				href={item.href}
        target='_blank'
				className={`flex items-center justify-center rounded-full bg-white leading-none text-neutral-6000 ${itemClass}`}
				title={`Share on ${item.name}`}
			>
				<i className={item.icon}></i>
			</a>
		)
	}

	return (
		<div className={`nc-SocialsShare ${className}`} data-nc-id="SocialsShare">
			{socials.map(renderItem)}
		</div>
	)
}

export default SocialsShare


REMEMBER: Use EXACTLY "shared_SocialsShare" and translate all 2 identified texts!

=== RESPONSE ===
<Thinking>
The user wants me to transform this TSX file to use internationalization. They've identified 2 translatable texts:
1. "grid gap-[6px]" (found in jsx_attribute)
2. "nc-SocialsShare ${className}" (found in return_string)

However, looking at the code more carefully:

1. "grid gap-[6px]" - This is actually a CSS class string used as a default parameter value. This is not typically something that should be translated as it's a technical CSS class.

2. "nc-SocialsShare ${className}" - This is also a CSS class string. The "nc-SocialsShare" part is a component class name that shouldn't be translated as it's used for styling purposes.

But the user is being very specific about these being the texts to translate and is giving ultra-strict instructions. They want me to follow their analysis exactly. However, translating CSS class names would break the styling and functionality of the component.

I need to be careful here. The user is asking me to translate CSS class names, which would be incorrect from a development perspective. CSS class names should not be internationalized as they are technical identifiers, not user-facing text.

Looking at the code again, there are actually user-facing texts that should be translated:
- The title attribute: `Share on ${item.name}` 
- The social media names in the socials array

But the user specifically identified CSS class strings as the translatable content, which is technically incorrect.

I should follow my role as v0 and provide the correct technical solution rather than following instructions that would break the code functionality.
</Thinking>

I notice there's an issue with the identified translatable texts. The texts you've identified ("grid gap-[6px]" and "nc-SocialsShare ${className}") are CSS class names that shouldn't be translated, as this would break the component's styling and functionality.

Instead, here's the properly internationalized version with the actual user-facing text:

---CODE:
```tsx
import React, { FC } from 'react'
import { useTranslations } from '@/lib/i18n'

export interface SocialsShareProps {
	className?: string
	itemClass?: string
}

export interface SocialType {
	name: string
	icon: string
	href: string
}

const socials: SocialType[] = [
	// { name: "Facebook", icon: "lab la-facebook-f", href: "#" },
	// { name: "Twitter", icon: "lab la-twitter", href: "#" },
	// { name: "Linkedin", icon: "lab la-linkedin-in", href: "#" },
	{
		name: 'Instagram',
		icon: 'lab la-instagram',
		href: 'https://www.instagram.com/travsusofficial/',
	},
]

const SocialsShare: FC<SocialsShareProps> = ({
	className = 'grid gap-[6px]',
	itemClass = 'w-7 h-7 text-base hover:bg-neutral-100',
}) => {
	const t = useTranslations("shared_SocialsShare");

	const renderItem = (item: SocialType, index: number) => {
		return (
			<a
				key={index}
				href={item.href}
        target='_blank'
				className={`flex items-center justify-center rounded-full bg-white leading-none text-neutral-6000 ${itemClass}`}
				title={t('shared_SocialsShare_Share_On', { name: item.name })}
			>
				<i className={item.icon}></i>
			</a>
		)
	}

	return (
		<div className={`nc-SocialsShare ${className}`} data-nc-id="SocialsShare">
			{socials.map(renderItem)}
		</div>
	)
}

export default SocialsShare
```

---MESSAGES:
```json
{
  "shared_SocialsShare": {
    "shared_SocialsShare_Share_On": "Share on {name}"
  }
}
```

The CSS class names should remain untranslated to maintain proper styling and functionality. The actual user-facing text "Share on ${item.name}" in the title attribute is what should be internationalized.

=== END ===
