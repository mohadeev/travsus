
=== FILE: C:\Users\Usuario\s\travsus\src\shared\Badge.tsx ===
=== NAMESPACE: shared_Badge ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_Badge"
- The useTranslations call MUST be: useTranslations("shared_Badge")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_Badge");
3. Replace static text with: t('shared_Badge_Key_Name')
4. Generate keys like: shared_Badge_Welcome_Message, shared_Badge_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_Badge")
✓ Keys start with namespace: shared_Badge_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_Badge");
  return <div>{t('shared_Badge_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_Badge": {
    "shared_Badge_Key1": "Original text 1",
    "shared_Badge_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import { TwMainColor } from "@/data/types";
import { Route } from "@/routers/types";
import Link from "next/link";
import React, { FC, ReactNode } from "react";

export interface BadgeProps {
  className?: string;
  name: ReactNode;
  color?: TwMainColor;
  href?: Route<string>;
}

const Badge: FC<BadgeProps> = ({
  className = "relative",
  name,
  color = "blue",
  href,
}) => {
  const getColorClass = (hasHover = true) => {
    switch (color) {
      case "pink":
        return `text-pink-800 bg-pink-100 ${
          hasHover ? "hover:bg-pink-800" : ""
        }`;
      case "red":
        return `text-red-800 bg-red-100 ${hasHover ? "hover:bg-red-800" : ""}`;
      case "gray":
        return `text-gray-800 bg-gray-100 ${
          hasHover ? "hover:bg-gray-800" : ""
        }`;
      case "green":
        return `text-green-800 bg-green-100 ${
          hasHover ? "hover:bg-green-800" : ""
        }`;
      case "purple":
        return `text-purple-800 bg-purple-100 ${
          hasHover ? "hover:bg-purple-800" : ""
        }`;
      case "indigo":
        return `text-indigo-800 bg-indigo-100 ${
          hasHover ? "hover:bg-indigo-800" : ""
        }`;
      case "yellow":
        return `text-yellow-800 bg-yellow-100 ${
          hasHover ? "hover:bg-yellow-800" : ""
        }`;
      case "blue":
        return `text-blue-800 bg-blue-100 ${
          hasHover ? "hover:bg-blue-800" : ""
        }`;
      default:
        return `text-pink-800 bg-pink-100 ${
          hasHover ? "hover:bg-pink-800" : ""
        }`;
    }
  };

  const CLASSES =
    "nc-Badge inline-flex px-2.5 py-1 rounded-full font-medium text-xs " +
    className;
  return !!href ? (
    <Link
      href={href || ""}
      className={`transition-colors hover:text-white duration-300 ${CLASSES} ${getColorClass()}`}
    >
      {name}
    </Link>
  ) : (
    <span className={`${CLASSES} ${getColorClass(false)} ${className}`}>
      {name}
    </span>
  );
};

export default Badge;


REMEMBER: Use EXACTLY "shared_Badge" - do not change it!

=== RESPONSE ===
<Thinking>
Looking at this TSX file, I need to transform it to use internationalization following the exact requirements:

1. Add the import: `import { useTranslations } from '@/lib/i18n'`
2. Add the hook call: `const t = useTranslations("shared_Badge");`
3. Replace static text with translation keys

However, looking at this Badge component, I notice that it doesn't actually contain any static text strings that need to be translated. The component:

- Takes a `name` prop which is a ReactNode (so it's passed from parent)
- Has color logic but no hardcoded text
- Has CSS classes but no user-facing text
- The only text-like content is the `name` prop which comes from outside

Since there are no static text strings to translate in this component, I should still add the internationalization setup as requested, but there won't be any actual translation keys to use. The component structure will remain the same but with the i18n setup added.

Wait, let me double-check... No, there really aren't any hardcoded strings that need translation. The `name` prop is passed in from the parent component, so that's where the translation would happen, not in this Badge component itself.

But following the instructions exactly, I need to add the import and hook call even if there's nothing to translate in this particular component.
</Thinking>

---CODE:
```tsx
import { useTranslations } from '@/lib/i18n'
import { TwMainColor } from "@/data/types";
import { Route } from "@/routers/types";
import Link from "next/link";
import React, { FC, ReactNode } from "react";

export interface BadgeProps {
  className?: string;
  name: ReactNode;
  color?: TwMainColor;
  href?: Route<string>;
}

const Badge: FC<BadgeProps> = ({
  className = "relative",
  name,
  color = "blue",
  href,
}) => {
  const t = useTranslations("shared_Badge");
  
  const getColorClass = (hasHover = true) => {
    switch (color) {
      case "pink":
        return `text-pink-800 bg-pink-100 ${
          hasHover ? "hover:bg-pink-800" : ""
        }`;
      case "red":
        return `text-red-800 bg-red-100 ${hasHover ? "hover:bg-red-800" : ""}`;
      case "gray":
        return `text-gray-800 bg-gray-100 ${
          hasHover ? "hover:bg-gray-800" : ""
        }`;
      case "green":
        return `text-green-800 bg-green-100 ${
          hasHover ? "hover:bg-green-800" : ""
        }`;
      case "purple":
        return `text-purple-800 bg-purple-100 ${
          hasHover ? "hover:bg-purple-800" : ""
        }`;
      case "indigo":
        return `text-indigo-800 bg-indigo-100 ${
          hasHover ? "hover:bg-indigo-800" : ""
        }`;
      case "yellow":
        return `text-yellow-800 bg-yellow-100 ${
          hasHover ? "hover:bg-yellow-800" : ""
        }`;
      case "blue":
        return `text-blue-800 bg-blue-100 ${
          hasHover ? "hover:bg-blue-800" : ""
        }`;
      default:
        return `text-pink-800 bg-pink-100 ${
          hasHover ? "hover:bg-pink-800" : ""
        }`;
    }
  };

  const CLASSES =
    "nc-Badge inline-flex px-2.5 py-1 rounded-full font-medium text-xs " +
    className;
  return !!href ? (
    <Link
      href={href || ""}
      className={`transition-colors hover:text-white duration-300 ${CLASSES} ${getColorClass()}`}
    >
      {name}
    </Link>
  ) : (
    <span className={`${CLASSES} ${getColorClass(false)} ${className}`}>
      {name}
    </span>
  );
};

export default Badge;
```

---MESSAGES:
```json
{
  "shared_Badge": {}
}
```

Note: This Badge component doesn't contain any hardcoded text strings that require translation. The `name` prop is passed from parent components, so translations would be handled at the parent level where the actual text content is defined. The internationalization setup has been added as requested, but there are no static strings in this component to translate.

=== END ===
