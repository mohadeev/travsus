
=== FILE: C:\Users\Usuario\s\travsus\src\shared\ButtonPrimary.tsx ===
=== NAMESPACE: shared_ButtonPrimary ===
=== PROMPT ===
# ULTRA-STRICT INTERNATIONALIZATION INSTRUCTIONS

Transform this TSX file to use internationalization. Follow these rules EXACTLY:

## CRITICAL NAMESPACE REQUIREMENT:
- You MUST use the exact namespace: "shared_ButtonPrimary"
- The useTranslations call MUST be: useTranslations("shared_ButtonPrimary")
- Do NOT modify this namespace in any way
- Do NOT use any other namespace

## EXACT REQUIREMENTS:
1. Add this import: import { useTranslations } from '@/lib/i18n'
2. Add this hook call: const t = useTranslations("shared_ButtonPrimary");
3. Replace static text with: t('shared_ButtonPrimary_Key_Name')
4. Generate keys like: shared_ButtonPrimary_Welcome_Message, shared_ButtonPrimary_Button_Text

## VALIDATION CHECKLIST:
✓ Import added: import { useTranslations } from '@/lib/i18n'
✓ Hook called with EXACT namespace: useTranslations("shared_ButtonPrimary")
✓ Keys start with namespace: shared_ButtonPrimary_*
✓ No dots in keys (use underscores)
✓ Complete file transformation

## EXAMPLE TRANSFORMATION:
BEFORE:
export default function MyComponent() {
  return <div>Hello World</div>
}

AFTER:
import { useTranslations } from '@/lib/i18n'

export default function MyComponent() {
  const t = useTranslations("shared_ButtonPrimary");
  return <div>{t('shared_ButtonPrimary_Hello_World')}</div>
}

## OUTPUT FORMAT:
---CODE:
<COMPLETE TSX FILE WITH EXACT NAMESPACE>
---MESSAGES:
{
  "shared_ButtonPrimary": {
    "shared_ButtonPrimary_Key1": "Original text 1",
    "shared_ButtonPrimary_Key2": "Original text 2"
  }
}

## FILE TO TRANSFORM:
import Button, { ButtonProps } from './Button'
import React from 'react'

export interface ButtonPrimaryProps extends ButtonProps {}

const ButtonPrimary: React.FC<ButtonPrimaryProps> = ({
	className = '',
	...args
}) => {
	return (
		<Button
			className={`ttnc-ButtonPrimary bg-primary text-neutral-50 hover:bg-primary-700 disabled:bg-opacity-70 ${className}`}
			{...args}
		/>
	)
}

export default ButtonPrimary


REMEMBER: Use EXACTLY "shared_ButtonPrimary" - do not change it!

=== RESPONSE ===
<Thinking>
The user wants me to transform a TSX file to use internationalization with very specific requirements. Let me analyze the file:

The file is a ButtonPrimary component that extends a Button component. Looking at the code, I don't see any static text that needs to be internationalized - it's just a styled button component that passes props through to a base Button component.

However, the user is asking me to follow their internationalization instructions exactly, including:
1. Adding the import: `import { useTranslations } from '@/lib/i18n'`
2. Adding the hook call: `const t = useTranslations("shared_ButtonPrimary");`
3. Using the exact namespace "shared_ButtonPrimary"

But there's no actual text content in this component to internationalize. The only text-like content is CSS classes and the component name/interface name, which shouldn't be internationalized.

Since there's no user-facing text to internationalize in this component, I should still follow the format requested but note that there are no translatable strings. I'll add the required import and hook call as requested, but there won't be any actual t() calls since there's no text to translate.

For the messages object, it will be empty since there are no translatable strings.
</Thinking>

---CODE:
```tsx
import { useTranslations } from '@/lib/i18n'
import Button, { ButtonProps } from './Button'
import React from 'react'

export interface ButtonPrimaryProps extends ButtonProps {}

const ButtonPrimary: React.FC<ButtonPrimaryProps> = ({
	className = '',
	...args
}) => {
	const t = useTranslations("shared_ButtonPrimary");
	
	return (
		<Button
			className={`ttnc-ButtonPrimary bg-primary text-neutral-50 hover:bg-primary-700 disabled:bg-opacity-70 ${className}`}
			{...args}
		/>
	)
}

export default ButtonPrimary
```

---MESSAGES:
```json
{
  "shared_ButtonPrimary": {}
}
```

Note: This component contains no user-facing text content to internationalize. It's a styled wrapper component that only passes through props and applies CSS classes. The internationalization setup has been added as requested, but there are no translatable strings in this particular component.

=== END ===
