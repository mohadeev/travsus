services:
  travsus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travsus
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.local